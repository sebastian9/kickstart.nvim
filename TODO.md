# TODO

* Add copilot config to copy command
* Compile or install kickstart dependencies such as ripgrep
  * Add rust to compile ripgrep
* Test node feature solves Mason install issues
* Move devcontainer image to dedicated spot in monorepo
* Add devcontainer image to artifactory
* Refactor ebt devc docker-compose to use new image
